services:
  - type: web
    name: llm-drift-monitor-api
    env: python
    buildCommand: pip install uv && uv sync
    startCommand: uv run uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
